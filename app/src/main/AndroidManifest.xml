<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".admin.ReservasPendientes"/>
        <activity android:name=".admin.CrearCanchas" />
        <activity android:name=".admin.MainActivity" />
        <activity android:name=".cliente.reservas" />
        <activity android:name=".cliente.busqueda" />
        <activity android:name=".cliente.MainActivity" />
        <activity android:name=".vista_principal.Registro" />
        <activity android:name=".vista_principal.Login" />
        <activity android:name=".vista_principal.MainActivity"/>
        <activity android:name=".vista_principal.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>